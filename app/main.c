#include <stdio.h>
#include "square.h"
#include "myfunc.h"

int main()
{
    printf("Hello World!\nВведите коэффиценты A, B, C\n");
    double a=0, b=0, c=0;
    square(a, b, c);
    myfunc(2);
    return 0;
}
